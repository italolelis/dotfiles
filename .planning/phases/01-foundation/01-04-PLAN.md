---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - git/.gitconfig
  - git/.gitignore_global
  - macos.sh
autonomous: true
requirements:
  - TOOL-03
  - TOOL-04

must_haves:
  truths:
    - "Git config has signing key, useful aliases, and references the global gitignore"
    - "Global gitignore covers common OS and editor artifacts"
    - "core.excludesfile in .gitconfig correctly references ~/.gitignore_global (works as symlink)"
    - "macos.sh contains only Sequoia-compatible defaults write commands"
    - "macos.sh has no references to macOS Tahoe or deprecated System Preferences"
    - "macos.sh is separate from install.sh (run manually)"
    - "All deprecated Homebrew tap commands removed from macos.sh"
  artifacts:
    - path: "git/.gitconfig"
      provides: "Git configuration with signing, aliases, global gitignore"
      contains: "excludesfile"
    - path: "git/.gitignore_global"
      provides: "Global gitignore for OS/editor artifacts"
    - path: "macos.sh"
      provides: "Sequoia-compatible macOS defaults script"
      min_lines: 30
  key_links:
    - from: "git/.gitconfig"
      to: "~/.gitignore_global"
      via: "core.excludesfile = ~/.gitignore_global"
      pattern: "excludesfile.*gitignore_global"
    - from: "git/.gitconfig"
      to: "GPG signing"
      via: "user.signingkey and commit.gpgsign"
      pattern: "signingkey"
---

<objective>
Audit and finalize the Git config (signing key, aliases, global gitignore) within the git/ Stow package, and audit the macOS defaults script for Sequoia compatibility.

Purpose: Git config must work correctly when symlinked (core.excludesfile path), and the macOS defaults script must not contain deprecated or wrong-version commands that fail silently on Sequoia.

Output: A verified git/.gitconfig, git/.gitignore_global, and a cleaned-up macos.sh.
</objective>

<execution_context>
@/Users/italovietro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/italovietro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and finalize Git config in git/ Stow package</name>
  <files>
    git/.gitconfig
    git/.gitignore_global
  </files>
  <action>
    The .gitconfig and .gitignore_global were moved to git/ in plan 01-01. Now verify they work correctly when symlinked.

    **1a. Audit git/.gitconfig:**

    Read the current git/.gitconfig and verify:
    - `[user]` section has name, email, signingkey (5CB8AEE431026C4C — this is a public key fingerprint, confirmed safe)
    - `[commit]` section has `gpgsign = true`
    - `[core]` section has `excludesfile = ~/.gitignore_global` — this path MUST work when .gitconfig is a symlink. Since Stow creates `~/.gitconfig -> ~/.dotfiles/git/.gitconfig`, and Git reads `~/.gitconfig`, the `~/.gitignore_global` path resolves correctly (it will also be a symlink). No change needed.
    - Aliases section exists with useful shortcuts

    If `core.excludesfile` uses a different path format (e.g., absolute path like `/Users/italovietro/.gitignore_global`), change it to `~/.gitignore_global` for portability across machines.

    **1b. Audit git/.gitignore_global:**

    Read the current git/.gitignore_global and verify it covers:
    - OS artifacts: `.DS_Store`, `Thumbs.db`, `._*`
    - Editor artifacts: `.idea/`, `.vscode/`, `*.swp`, `*.swo`, `*~`
    - Runtime: `*.pyc`, `__pycache__/`, `node_modules/`

    If any common patterns are missing, add them. Keep the file focused — this is a global gitignore, not a project-specific one.

    **1c. Verify symlink compatibility:**

    The key insight: when `~/.gitconfig` is a symlink to `~/.dotfiles/git/.gitconfig`, Git reads the file content (following the symlink). Paths inside .gitconfig like `~/.gitignore_global` resolve relative to `$HOME`, not relative to the symlink target. So `excludesfile = ~/.gitignore_global` is correct. No special handling needed.

    Confirm this by checking: `git config --global core.excludesfile` should output a path (this command reads the current $HOME/.gitconfig, whether it's a real file or symlink).
  </action>
  <verify>
    <automated>cd /Users/italovietro/.dotfiles && grep -q "excludesfile" git/.gitconfig && echo "excludesfile configured" && grep -q "signingkey" git/.gitconfig && echo "signing key present" && grep -q "DS_Store" git/.gitignore_global && echo "global gitignore has OS patterns" && echo "Git config verified"</automated>
  </verify>
  <done>git/.gitconfig has signing key, aliases, and correct excludesfile path (~/.gitignore_global). git/.gitignore_global covers OS/editor artifacts. Both files are ready for Stow symlink deployment.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and clean macOS defaults script for Sequoia compatibility</name>
  <files>macos.sh</files>
  <action>
    The existing .macos file was renamed to macos.sh in the repo root (it stays at root, not in a Stow package — per CONTEXT.md it's a separate manual step).

    Wait — check: the file is currently at `~/.dotfiles/.macos` (tracked) and there's also `~/.dotfiles/macos.sh`. Read both to understand which is the main one.

    **Current state from research:**
    - `.macos` (~295 lines, at repo root) — the existing defaults script
    - `macos.sh` (at repo root) — appears to be a separate install-related script

    Read both files. The `.macos` script is the macOS defaults script that needs auditing. The `macos.sh` may be the installer wrapper. Per CONTEXT.md: "macos.sh is a SEPARATE step from install.sh — run manually when ready."

    **Audit process for the macOS defaults script:**

    1. **Remove all Tahoe references** (RESEARCH.md Pitfall 5):
       - Search for "Tahoe", "26.0", "Stage Manager (new in Tahoe)", "LiveActivities"
       - Remove or replace with Sequoia-appropriate comments

    2. **Fix deprecated references:**
       - Replace "System Preferences" with "System Settings" in any osascript calls (renamed in macOS Ventura+, per RESEARCH.md)
       - Remove deprecated `brew tap homebrew/bundle` if present in macos.sh (RESEARCH.md Pitfall 3)
       - Remove `brew upgrade` if present (RESEARCH.md anti-pattern — non-idempotent)

    3. **Audit defaults write commands by category (per CONTEXT.md priority):**

       **Finder & Desktop** — Keep commands that set:
       - Show hidden files, path bar, status bar
       - Column view as default
       - Search current folder by default
       - Show file extensions

       **Dock & Mission Control** — Keep commands that set:
       - Dock size, autohide, minimize effect
       - Hot corners (if any)
       - Remove recent applications from Dock

       **Keyboard & Input** — Keep commands that set:
       - Key repeat rate and delay
       - Disable auto-correct, smart quotes, smart dashes (developer preference)

       **Security & Privacy** — Keep commands that set:
       - Firewall enable
       - Require password after sleep

       **Remove if present:**
       - Any commands with domains that don't exist on Sequoia
       - Any commands setting values for removed features
       - Redundant or duplicate commands

    4. **Ensure macos.sh is the final script name** — if the defaults live in `.macos`, consolidate into `macos.sh`. Remove `.macos` if it's superseded (use `git rm .macos` to track removal). If both files serve different purposes, keep both but clarify.

    5. **Add header comment** to macos.sh:
       ```bash
       #!/usr/bin/env bash
       # macOS defaults — run manually after install.sh
       # Tested on: macOS Sequoia (15.x)
       # Usage: ./macos.sh
       ```

    6. **Ensure script ends with:**
       ```bash
       echo "Done. Some changes require a logout/restart to take effect."
       ```

    **Note on Sequoia compatibility verification:** The definitive reference is macos-defaults.com. For commands where compatibility is uncertain, add a comment noting uncertainty. The machine IS running Sequoia (Darwin 25.4.0), so commands can be tested directly if needed.
  </action>
  <verify>
    <automated>cd /Users/italovietro/.dotfiles && test -f macos.sh && echo "macos.sh exists" && head -1 macos.sh | grep -q "bash" && echo "Correct shebang" && ! grep -qi "tahoe" macos.sh && echo "No Tahoe references" && ! grep -q "System Preferences" macos.sh && echo "No deprecated System Preferences" && ! grep -q 'brew tap homebrew/bundle' macos.sh && echo "No deprecated tap" && bash -n macos.sh && echo "Syntax valid"</automated>
  </verify>
  <done>macos.sh is a clean, Sequoia-compatible defaults script. No Tahoe references, no deprecated System Preferences, no deprecated brew taps. Organized by category (Finder, Dock, Keyboard, Security). Passes bash syntax check. Separate from install.sh (run manually).</done>
</task>

</tasks>

<verification>
1. `grep "excludesfile" git/.gitconfig` — shows `~/.gitignore_global` path
2. `grep "signingkey" git/.gitconfig` — shows signing key configured
3. `grep "gpgsign" git/.gitconfig` — shows commit signing enabled
4. `bash -n macos.sh` — no syntax errors
5. `grep -i "tahoe" macos.sh` — should match nothing
6. `grep "System Preferences" macos.sh` — should match nothing
7. `grep "brew tap" macos.sh` — should match nothing (no deprecated taps)
8. `head -3 macos.sh` — shows bash shebang and Sequoia compatibility comment
</verification>

<success_criteria>
- git/.gitconfig has user.signingkey, commit.gpgsign, core.excludesfile pointing to ~/.gitignore_global
- git/.gitignore_global covers .DS_Store, editor artifacts, runtime artifacts
- macos.sh contains only Sequoia-compatible defaults
- No Tahoe (26.0) references anywhere
- No "System Preferences" references (use "System Settings")
- No deprecated brew tap commands
- macos.sh passes bash -n syntax check
- macos.sh is separate from install.sh (confirmed by CONTEXT.md)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
