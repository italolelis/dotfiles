---
quick_id: 260426-gn1
slug: cship-integration
date: 2026-04-26
description: Verify starship is well-installed and add cship (Claude Code statusline) to dotfiles management
---

# Quick Task 260426-gn1: cship integration

## Goal

Bring `cship` (Claude Code statusline renderer that consumes starship config) under dotfiles management, on par with how starship is managed.

## Current State

- **starship**: installed via Homebrew, in Brewfile, config stowed (`starship/.starship.toml` → `~/.starship.toml`), `STARSHIP_CONFIG` env in `zsh/.exports`, init in `zsh/.zshrc`. ✅ Already well integrated.
- **cship**: binary at `~/.local/bin/cship` (1.5.1, ARM64 Mach-O) — installed manually, NOT in dotfiles.
- **cship config**: regular file at `~/.config/cship.toml` — NOT stowed, would be lost on a fresh machine.
- **Claude Code statusLine**: `~/.claude/settings.json` already has `{ "type": "command", "command": "cship" }`. ✅

## Plan (1 task)

### Task: Stow cship config + add install_cship to install.sh

**Files:**
- `cship/.config/cship.toml` (new — content moved from `~/.config/cship.toml`)
- `install.sh` (add `install_cship` function, add `cship` to `PACKAGES`)

**Action:**
1. Create `cship/` stow package with the existing `~/.config/cship.toml` content.
2. Add `install_cship()` function to `install.sh` — uses official curl installer (`https://cship.dev/install.sh`), idempotent (skips if already installed). Runs on both macOS and Linux since cship is not in Homebrew.
3. Add `cship` to `PACKAGES` array so stow includes it.
4. Run `stow cship` locally to materialize the symlink.

**Verify:**
- `ls -la ~/.config/cship.toml` shows symlink to `.dotfiles/cship/.config/cship.toml`
- `cship --version` still works
- `cship explain` resolves config from the new path
- `install.sh` is shellcheck-clean

**Done when:**
- `cship` package is in dotfiles and stowed
- `install.sh` will reproduce the cship install + config on a fresh machine
- No regression to starship setup
