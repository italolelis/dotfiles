---
phase: 02-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - zsh/.path
  - zsh/.exports
  - zsh/.aliases
  - zsh/.functions
  - zsh/.zsh_completions
autonomous: true
requirements:
  - SHEL-03
  - SHEL-05

must_haves:
  truths:
    - ".path has no duplicate PATH entries and all PATH modifications live only in .path"
    - ".exports has STARSHIP_CONFIG pointing to ~/.starship.toml and no PATH modifications"
    - ".aliases has no Warp, kubectl, or broken Python 2 references"
    - ".functions has no stale Python 2 code or dead utilities"
    - ".zsh_completions has no compinit call and no kubectl completion"
  artifacts:
    - path: "zsh/.path"
      provides: "Deduplicated PATH declarations, single source of truth for PATH"
    - path: "zsh/.exports"
      provides: "Clean env vars with STARSHIP_CONFIG, no PATH modifications"
      contains: "STARSHIP_CONFIG"
    - path: "zsh/.aliases"
      provides: "Cleaned aliases without Warp/kubectl, fixed urlencode, simplified update"
    - path: "zsh/.functions"
      provides: "Shell functions without stale code"
    - path: "zsh/.zsh_completions"
      provides: "Custom completions without compinit call or kubectl, with compdef registrations only"
  key_links:
    - from: "zsh/.exports"
      to: "starship/.starship.toml"
      via: "STARSHIP_CONFIG env var tells Starship where to find its config"
      pattern: "STARSHIP_CONFIG.*\\.starship\\.toml"
---

<objective>
Clean up all zsh support files to remove dead code, fix broken utilities, deduplicate PATH entries, and wire Starship config via STARSHIP_CONFIG env var.

Purpose: The .zshrc rewrite (plan 02-01) sources these files. They must be cleaned of OMZ-era cruft (Warp aliases, kubectl aliases, SSH agent references, Python 2 code, duplicate PATH entries, redundant compinit calls) to match the new shell architecture. The STARSHIP_CONFIG env var in .exports wires the existing starship/.starship.toml Stow package to Starship without restructuring the package.

Output: Cleaned .path, .exports, .aliases, .functions, .zsh_completions.
</objective>

<execution_context>
@/Users/italovietro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/italovietro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell/02-RESEARCH.md
@.planning/phases/02-shell/02-CONTEXT.md

Source files to clean:
@zsh/.path
@zsh/.exports
@zsh/.aliases
@zsh/.functions
@zsh/.zsh_completions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deduplicate .path and clean .exports</name>
  <files>zsh/.path, zsh/.exports</files>
  <action>
**zsh/.path cleanup:**

Audit every PATH entry in .path. The file currently has these issues (from RESEARCH.md Pitfall 8):
- `$HOME/.local/bin` appears TWICE (lines 5 and 14)
- `$HOME/.npm-global/bin` appears in BOTH .path (line 17) and .exports (line 24)
- `$HOME/go/bin` appears in BOTH .path (line 8) and .exports (line 12)

Fix by making .path the SINGLE source of truth for all PATH additions:
1. Remove the duplicate `$HOME/.local/bin` entry (keep only one)
2. Keep `$HOME/.npm-global/bin` in .path, remove from .exports
3. Keep `$HOME/go/bin` in .path, remove from .exports
4. Remove the `#!/usr/bin/env bash` shebang — this is a zsh-sourced file, not an executable script
5. Keep all conditional PATH additions (Android, Flutter, Deno, Bun) — they're already guarded with `[ -d ... ]`
6. Keep the Homebrew Intel/ARM detection block — Phase 3 will add Linux branch

The resulting .path should have each PATH entry exactly once.

**zsh/.exports cleanup:**

1. Remove `export PATH=$HOME/go/bin:$PATH` (line 12) — moved to .path
2. Remove `export PATH="$HOME/.npm-global/bin:$PATH"` (line 24) — moved to .path
3. Add `export STARSHIP_CONFIG="$HOME/.starship.toml"` — tells Starship where to find config (SHEL-05, Option A from RESEARCH.md Pattern 4). Place after the EDITOR/VISUAL/PAGER block.
4. Remove `export PYTHONPATH="${PYTHONPATH}:${HOME}/.local/lib/python3.11/site-packages"` (line 27) — non-standard, causes issues with virtualenvs, remove unless specifically needed (RESEARCH.md State of the Art: "remove unless specifically needed")
5. Keep `export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES` for now — may still be needed; flag with comment "# TODO: verify if still needed on current macOS"
6. Keep `export USE_GKE_GCLOUD_AUTH_PLUGIN=True` — gcloud is in Brewfile (RESEARCH.md Open Question 3: "keep for now")
7. Remove `export HISTSIZE`, `export HISTFILESIZE`, `export HISTCONTROL` — these are bash-specific; zsh history is configured via setopts in .zshrc (plan 02-01 handles this)
8. Remove the `#!/usr/bin/env bash` shebang if present — this is sourced by zsh
9. Keep LANG, LC_ALL, EDITOR, VISUAL, PAGER, NODE_ENV, npm_config_prefix, DOCKER_BUILDKIT, COMPOSE_DOCKER_CLI_BUILD, HOMEBREW_NO_ANALYTICS, HOMEBREW_NO_INSECURE_REDIRECT
  </action>
  <verify>
    <automated>grep -c 'local/bin' zsh/.path | xargs test 1 -eq && ! grep -q 'go/bin\|npm-global' zsh/.exports && grep -q 'STARSHIP_CONFIG' zsh/.exports && ! grep -q 'HISTSIZE\|HISTFILESIZE\|HISTCONTROL' zsh/.exports && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.path has zero duplicate PATH entries (each directory appears exactly once). .exports has no PATH modifications (all moved to .path). .exports contains STARSHIP_CONFIG="$HOME/.starship.toml". Bash-specific HISTSIZE/HISTFILESIZE/HISTCONTROL removed from .exports.</done>
</task>

<task type="auto">
  <name>Task 2: Clean .aliases, .functions, and .zsh_completions</name>
  <files>zsh/.aliases, zsh/.functions, zsh/.zsh_completions</files>
  <action>
**zsh/.aliases cleanup (per CONTEXT.md locked decisions):**

REMOVE these aliases:
- `alias w="warp"` and `alias wd="warp --directory"` (lines 24-25) — Warp is dead, Ghostty is terminal
- `alias k="kubectl"`, `alias kctx="kubectx"`, `alias kns="kubens"` (lines 31-33) — kubectl dropped per CONTEXT.md
- `alias speedtest=...` (line 36) — uses `python -` which is Python 2; broken on modern macOS

FIX these aliases:
- `alias urlencode=...` (line 112) — currently uses Python 2 urllib syntax. Replace with:
  `alias urlencode='python3 -c "import sys, urllib.parse; print(urllib.parse.quote_plus(sys.argv[1]))"'`
- `alias update=...` (line 75) — simplify to just `brew update && brew upgrade && brew cleanup` per CONTEXT.md decision ("simplify update alias to just brew update + cleanup; Phase 4 dot command will be the real updater"). Remove sudo softwareupdate, npm, gem portions.

KEEP everything else (navigation aliases, macOS Finder aliases, Docker aliases dc/dcu/dcd, ls/grep color aliases, network utilities, cleanup aliases, etc.).

Remove the `#!/usr/bin/env bash` shebang at line 1 — this is sourced by zsh, not executed.

**zsh/.functions cleanup:**

- Remove the `#!/usr/bin/env bash` shebang at line 1 — sourced by zsh
- Keep all functions as-is. They are all valid and useful: mkd, cdf, targz, fs, diff, dataurl, gz, digga, getcertnames, o, tre, devserver, gitinit, dockerclean, portcheck, sysinfo, backup.
- The `open` normalization block (lines 127-135) is fine — it handles Linux/WSL cross-platform.
- No Python 2 code exists in .functions (the functions use python3 where needed).

**zsh/.zsh_completions cleanup:**

This file currently:
1. Calls `autoload -Uz compinit` and `compinit` inside an anonymous function (lines 50-60) — REMOVE this. ez-compinit (loaded by antidote in plan 02-01) handles compinit. Double compinit kills cache performance (RESEARCH Pitfall 1).
2. Has `_kctx` kubectl context completion (lines 26-30) — REMOVE. kubectl dropped.
3. Has `compdef _kctx kctx` registration (line 57) — REMOVE.
4. Has `_dc` docker compose completion, `_git_branch` completion — KEEP these.

Rewrite .zsh_completions to contain ONLY:
- The `_dc` function definition
- The `_git_branch` function definition
- `compdef _dc dc` registration (no wrapper — ez-compinit handles the compdef-before-compinit problem)
- `compdef _git_branch gco` and `compdef _git_branch gb` registrations

NOTE: .zsh_completions is no longer sourced by .zshrc (plan 02-01 removed it from the source loop). However, the custom completions need a new home. The cleanest approach: keep the file but rename the sourcing approach. Add these compdef registrations directly at the bottom of the .zsh_completions file WITHOUT the anonymous function wrapper and WITHOUT compinit calls. Then add `.zsh_completions` back to the .zshrc source loop AFTER the antidote block.

**REVISED approach:** Since ez-compinit wraps compdef to work before compinit, the compdef calls in .zsh_completions will work correctly if the file is sourced AFTER the antidote block. However, the .zshrc source loop runs BEFORE antidote (step 2 in the load order).

**FINAL approach:** Move the custom completion function definitions (_dc, _git_branch) and their compdef registrations into .zsh_completions. Remove the anonymous function wrapper and compinit calls. The file will need to be sourced AFTER antidote loads. Since plan 02-01's .zshrc sources dotfiles before antidote, add a single line in .zshrc after the antidote block: `[[ -r ~/.zsh_completions ]] && source ~/.zsh_completions`

**IMPORTANT:** This means plan 02-01's .zshrc needs a small addition. Add this note for the executor: after the antidote source block (step 7) and before the key bindings (step 8), add:
```zsh
# Custom completions (sourced after antidote/ez-compinit handles compinit)
[[ -r ~/.zsh_completions ]] && source ~/.zsh_completions
```
If plan 02-01 has already been executed, edit zsh/.zshrc to add this line. If not yet executed, the executor of plan 02-01 should include it.
  </action>
  <verify>
    <automated>! grep -q 'warp\|kubectl\|kubens\|kubectx' zsh/.aliases && grep -q 'python3.*urllib.parse' zsh/.aliases && ! grep -q 'compinit' zsh/.zsh_completions && ! grep -q '_kctx' zsh/.zsh_completions && grep -q '_dc' zsh/.zsh_completions && grep -q 'STARSHIP_CONFIG' zsh/.exports && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.aliases has no Warp aliases (w, wd), no kubectl aliases (k, kctx, kns), no broken speedtest, fixed urlencode using python3, simplified update alias. .functions is clean (shebangs removed). .zsh_completions has no compinit call, no _kctx function, retains _dc and _git_branch with bare compdef registrations.</done>
</task>

</tasks>

<verification>
1. `grep -c 'warp\|kubectl\|kubens\|kubectx' zsh/.aliases` returns 0
2. `grep 'urlencode' zsh/.aliases` shows python3 urllib.parse
3. `grep 'STARSHIP_CONFIG' zsh/.exports` shows correct path
4. `grep -c 'HISTSIZE\|HISTCONTROL\|go/bin\|npm-global' zsh/.exports` returns 0
5. `grep -c 'compinit\|_kctx' zsh/.zsh_completions` returns 0
6. `sort zsh/.path | uniq -d` returns empty (no duplicate lines)
</verification>

<success_criteria>
- .path is the single source of truth for PATH with no duplicates
- .exports has STARSHIP_CONFIG and no PATH modifications
- .aliases has no Warp/kubectl references and urlencode uses python3
- .zsh_completions has no compinit call and no kubectl completions
- All files are clean of bash shebangs (they're sourced by zsh, not executed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell/02-02-SUMMARY.md`
</output>
