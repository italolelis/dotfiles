---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zsh/.zshrc
  - zsh/.aliases
  - zsh/.functions
  - zsh/.exports
  - zsh/.path
  - zsh/.extra
  - zsh/.zsh_completions
  - git/.gitconfig
  - git/.gitignore_global
  - tmux/.tmux.conf
  - starship/.starship.toml
  - ghostty/.config/ghostty/config
  - ssh/.ssh/config
  - ssh/.stow-local-ignore
  - misc/.editorconfig
  - misc/.inputrc
  - misc/.curlrc
  - misc/.wgetrc
  - .gitignore
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Every tracked dotfile lives inside a per-tool Stow package directory, not at the repo root"
    - "The repo has packages: zsh/, git/, tmux/, starship/, ghostty/, ssh/, misc/"
    - "Each package's internal layout mirrors the $HOME path (e.g., ghostty/.config/ghostty/config)"
    - "A .gitignore excludes .DS_Store, .backup/, and build artifacts"
    - "SSH package has .stow-local-ignore that blocks private keys from ever being stowed"
    - "No secrets exist in any tracked file (gitleaks passes)"
  artifacts:
    - path: "zsh/.zshrc"
      provides: "Shell config entry point"
    - path: "git/.gitconfig"
      provides: "Git configuration"
    - path: "ghostty/.config/ghostty/config"
      provides: "Ghostty terminal config in XDG layout"
    - path: "ssh/.ssh/config"
      provides: "SSH host config"
    - path: "ssh/.stow-local-ignore"
      provides: "Safety net blocking private keys from stow"
    - path: "misc/.editorconfig"
      provides: "Editor-agnostic formatting config"
    - path: ".gitignore"
      provides: "Repo-level ignore rules"
  key_links:
    - from: "zsh/"
      to: "$HOME/.zshrc, $HOME/.aliases, etc."
      via: "GNU Stow symlink creation"
      pattern: "stow.*zsh"
    - from: "ghostty/.config/ghostty/config"
      to: "$HOME/.config/ghostty/config"
      via: "GNU Stow with --no-folding"
      pattern: "stow.*--no-folding.*ghostty"
---

<objective>
Restructure the flat dotfiles repo into per-tool GNU Stow packages, add a repo .gitignore, create SSH safety ignore, and scan for secrets.

Purpose: This is the foundation for all subsequent plans. Stow requires files organized in package directories that mirror $HOME paths. Without this structure, nothing can be stowed.

Output: 7 Stow package directories (zsh/, git/, tmux/, starship/, ghostty/, ssh/, misc/) with all tracked files moved into correct locations, plus a .gitignore and ssh/.stow-local-ignore.
</objective>

<execution_context>
@/Users/italovietro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/italovietro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Stow package directories and move all tracked files into them</name>
  <files>
    zsh/.zshrc
    zsh/.aliases
    zsh/.functions
    zsh/.exports
    zsh/.path
    zsh/.extra
    zsh/.zsh_completions
    git/.gitconfig
    git/.gitignore_global
    tmux/.tmux.conf
    starship/.starship.toml
    ghostty/.config/ghostty/config
    ssh/.ssh/config
    misc/.editorconfig
    misc/.inputrc
    misc/.curlrc
    misc/.wgetrc
  </files>
  <action>
    Restructure the repo from flat layout to Stow packages. Use `git mv` for every file to preserve history.

    **Package layout (per CONTEXT.md locked decisions):**

    1. **zsh/** — All shell files (tightly coupled per user decision):
       - `git mv .zshrc zsh/.zshrc`
       - `git mv .aliases zsh/.aliases`
       - `git mv .functions zsh/.functions`
       - `git mv .exports zsh/.exports`
       - `git mv .path zsh/.path`
       - `git mv .extra zsh/.extra`
       - `git mv .zsh_completions zsh/.zsh_completions`

    2. **git/** — Git config:
       - `git mv .gitconfig git/.gitconfig`
       - `git mv .gitignore_global git/.gitignore_global`

    3. **tmux/** — Tmux config:
       - `git mv .tmux.conf tmux/.tmux.conf`

    4. **starship/** — Starship prompt:
       - `git mv .starship.toml starship/.starship.toml`

    5. **ghostty/** — Ghostty terminal (XDG path mirroring):
       - `mkdir -p ghostty/.config/ghostty`
       - `git mv .config/ghostty/config ghostty/.config/ghostty/config`
       - Remove empty `.config/ghostty/` and `.config/` dirs after move

    6. **ssh/** — SSH config (NOT private keys):
       - `mkdir -p ssh/.ssh`
       - `git mv .ssh/config ssh/.ssh/config`
       - Remove empty `.ssh/` dir after move

    7. **misc/** — Homeless files (Claude's discretion per CONTEXT.md):
       - `git mv .editorconfig misc/.editorconfig`
       - `git mv .inputrc misc/.inputrc`
       - `git mv .curlrc misc/.curlrc`
       - `git mv .wgetrc misc/.wgetrc`

    **Important:**
    - Use `git mv`, NOT `mv` — preserves git history
    - Create parent directories with `mkdir -p` BEFORE `git mv` for nested paths
    - After all moves, verify no tracked dotfiles remain at repo root (except install.sh, macos.sh, Brewfile, .gitignore, README.md, and directories)
    - Do NOT move install.sh, macos.sh, or install/Brewfile — those are handled in plans 01-02 and 01-03
    - Do NOT move .planning/ or .vscode/ — those stay at root
  </action>
  <verify>
    <automated>cd /Users/italovietro/.dotfiles && ls zsh/.zshrc git/.gitconfig tmux/.tmux.conf starship/.starship.toml ghostty/.config/ghostty/config ssh/.ssh/config misc/.editorconfig misc/.inputrc misc/.curlrc misc/.wgetrc && echo "All package files exist"</automated>
  </verify>
  <done>All 7 Stow package directories exist with correct internal file layout. No dotfiles remain at repo root (except install.sh, macos.sh, README.md, and planning/build dirs). git status shows renames, not deletes+adds.</done>
</task>

<task type="auto">
  <name>Task 2: Create .gitignore, ssh/.stow-local-ignore, and run gitleaks scan</name>
  <files>
    .gitignore
    ssh/.stow-local-ignore
  </files>
  <action>
    **2a. Create repo .gitignore** at `~/.dotfiles/.gitignore`:

    ```
    # OS
    .DS_Store
    .DS_Store?
    ._*
    .Spotlight-V100
    .Trashes
    Thumbs.db

    # Dotfiles management
    .backup/
    .planning/phases/**/*-SUMMARY.md
    .planning/phases/**/*-VERIFICATION.md
    .planning/phases/**/*-UAT.md

    # Editor
    .vscode/
    *.swp
    *.swo
    *~

    # Stow
    .stow-local-ignore
    ```

    **2b. Create ssh/.stow-local-ignore** (safety net — blocks private keys from stow even if they accidentally end up in the repo):

    ```
    # Override all defaults when this file exists — re-add what we want ignored
    \.git
    \.gitignore
    README.*

    # Private keys — NEVER stow these
    id_rsa
    id_rsa\.pub
    id_ed25519
    id_ed25519\.pub
    id_ecdsa
    id_ecdsa\.pub
    known_hosts
    known_hosts\.old
    authorized_keys
    ```

    Note: .stow-local-ignore uses regex patterns (not globs). When this file exists, it OVERRIDES Stow's default ignore list, so we must re-add .git and .gitignore.

    **2c. Run gitleaks scan** (per STATE.md blocker: "Audit every config file for secrets BEFORE any git add"):

    If `gitleaks` is installed (`command -v gitleaks`), run:
    ```bash
    gitleaks detect --source=. --no-git --verbose
    ```

    If gitleaks is NOT installed, manually verify:
    - .gitconfig signing key ID (5CB8AEE431026C4C) is a public key fingerprint — NOT a secret (safe)
    - .extra is a template with comments only — no real secrets
    - No API keys, tokens, or passwords in any tracked file

    Report findings but do NOT block on gitleaks installation (it's not in the Brewfile yet).
  </action>
  <verify>
    <automated>cd /Users/italovietro/.dotfiles && test -f .gitignore && echo ".gitignore exists" && test -f ssh/.stow-local-ignore && echo ".stow-local-ignore exists" && grep -q "DS_Store" .gitignore && grep -q "id_rsa" ssh/.stow-local-ignore && echo "Both files have expected content"</automated>
  </verify>
  <done>.gitignore blocks .DS_Store, .backup/, and editor artifacts. ssh/.stow-local-ignore blocks all private key patterns. No secrets found in tracked files (gitleaks clean or manual review confirms).</done>
</task>

</tasks>

<verification>
1. `ls -d zsh git tmux starship ghostty ssh misc` — all 7 package dirs exist
2. `ls zsh/.zshrc zsh/.aliases zsh/.functions zsh/.exports zsh/.path zsh/.extra zsh/.zsh_completions` — all shell files in zsh/
3. `ls ghostty/.config/ghostty/config` — XDG path correctly mirrored
4. `ls ssh/.ssh/config` — SSH config in correct nested path
5. `cat .gitignore | grep DS_Store` — .gitignore has OS exclusions
6. `cat ssh/.stow-local-ignore | grep id_rsa` — private key safety net in place
7. `git status` shows renames (not delete+add) for moved files
</verification>

<success_criteria>
- All tracked dotfiles live in per-tool Stow package directories
- Package layout matches CONTEXT.md locked decisions exactly
- .gitignore protects repo from OS/editor artifacts
- SSH .stow-local-ignore prevents private key stowing
- No secrets in tracked files
- Git history preserved for moved files (git mv used)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
